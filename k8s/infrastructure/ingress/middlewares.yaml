apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: forward-auth
  namespace: cerberus
spec:
  forwardAuth:
    address: http://security-gate.cerberus.svc.cluster.local:5275/api/auth/me
    authResponseHeaders:
      - X-User-Id
      - X-User-Email
      - X-User-Role
